pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'org.asciidoctor.jvm.convert' version '3.3.2'
        id 'com.palantir.graal' version '0.10.0'
    }
}

rootProject.name = "java-cli-template"